# ai-job-match
JobMatch AI is a full-stack web app that recommends jobs based on user skills or CV. It uses cosine similarity to match skills with job requirements and returns top roles with match percentages. Built using HTML, CSS, JavaScript, Node.js, and Express.
